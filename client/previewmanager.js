import { decodePosition, encodePosition } from "./transaction.js";
import { OP_PRESENCE, OP_TYPE } from "./shared/instructionset.js";

export default class PreviewManager {
    constructor(virtualCanvas, userId, input){
        
    }
}